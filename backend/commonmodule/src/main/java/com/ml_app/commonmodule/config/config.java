package com.ml_app.commonmodule.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.data.jpa.repository.config.EnableJpaAuditing;

@Configuration
@EnableJpaAuditing
public class config {
    // 설정 클래스이므로 몸체는 비어있어도 됩니다.
}
